const { Triangle, Circle, Square } = require('./shapes')

describe('Triangle', ()=>{
   Test('returns the right svg with corect color') 
});

describe('Circle', ()=>{
    Test('returns the right svg with corect color') 
 });

 describe('Square', ()=>{
    Test('returns the right svg with corect color') 
 });